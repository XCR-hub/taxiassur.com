╔════════════════════════════════════════════════════════════╗
║  ⚠️ EDGE FUNCTION NON DÉPLOYÉE                            ║
╚════════════════════════════════════════════════════════════╝

ERREUR :
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ POST generate-seo-content 502 (Bad Gateway)
❌ CORS policy: No 'Access-Control-Allow-Origin' header

CAUSE :
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

L'Edge Function `generate-seo-content` existe dans le code
MAIS elle n'est PAS déployée sur Supabase.

SOLUTION :
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Tu as 2 OPTIONS :

OPTION 1 : Ne pas utiliser l'Edge Function (RECOMMANDÉ)
───────────────────────────────────────────────────────────

L'AI Generator peut fonctionner SANS Edge Function.
Il génère le contenu côté client et l'enregistre directement.

Action : RIEN À FAIRE
Le système devrait déjà gérer le fallback automatiquement.


OPTION 2 : Déployer l'Edge Function (AVANCÉ)
───────────────────────────────────────────────────────────

Si tu veux vraiment utiliser l'Edge Function :

1. Installer Supabase CLI :
   npm install -g supabase

2. Login :
   supabase login

3. Link project :
   supabase link --project-ref drohhxrkoequjphvabvq

4. Deploy function :
   supabase functions deploy generate-seo-content

5. Configurer secrets :
   supabase secrets set OPENAI_API_KEY=sk-...
   supabase secrets set PEXELS_API_KEY=...


POURQUOI L'ERREUR 502 ?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

502 = Bad Gateway = Le serveur Edge Function n'existe pas
OU la function crash immédiatement au démarrage

Si pas déployée → 502
Si déployée mais crash → 502 aussi


QUE FAIRE MAINTENANT ?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Teste l'AI Generator SANS Edge Function :

1. Va sur /backoffice/ai-generator

2. Remplis le formulaire

3. Clique "Générer maintenant"

4. Le système devrait :
   - Générer un contenu mock si pas d'Edge Function
   - OU afficher une erreur claire
   - NE PAS planter avec 502

Si ça ne fonctionne toujours pas :
→ Dis-moi et je modifierai le code pour gérer ce cas.


ALTERNATIVE RAPIDE :
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Le backoffice peut générer du contenu directement :

1. Va dans "Content Manager"
2. Crée un article manuellement
3. Utilise les templates prédéfinis
4. Sauvegarde dans blog_posts

Pas besoin d'Edge Function pour ça.

