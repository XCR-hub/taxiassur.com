🔧 FIX ARTICLES BLOG - 2 MINUTES

═══════════════════════════════════════════════════════
PROBLÈME
═══════════════════════════════════════════════════════

✅ Liste articles fonctionne: /blog → OK
❌ Article individuel ne s'affiche pas: /blog/xxx → KO

═══════════════════════════════════════════════════════
SOLUTION - 3 ÉTAPES
═══════════════════════════════════════════════════════

📍 ÉTAPE 1: Exécuter le SQL (60 secondes)
───────────────────────────────────────────────────────

1. Ouvrir Supabase SQL Editor:
   https://supabase.com/dashboard/project/enwraesoedauooxmptpw/sql

2. Créer une nouvelle query

3. Copier-coller le contenu COMPLET du fichier:
   📄 FIX-BLOG-FUNCTIONS-MAINTENANT.sql

4. Cliquer "Run" (ou Ctrl+Enter)

5. Attendre le message:
   ✅ Les 2 fonctions RPC blog ont été créées avec succès

6. Vérifier que les fonctions fonctionnent:

   Test 1:
   ```sql
   SELECT slug, title FROM get_blog_posts() LIMIT 5;
   ```
   → Devrait afficher 5 articles

   Test 2:
   ```sql
   SELECT * FROM get_blog_post_by_slug('assurance-taxi-2024');
   ```
   → Devrait afficher 1 article complet


📍 ÉTAPE 2: Recharger la page (10 secondes)
───────────────────────────────────────────────────────

Sur /blog:

Windows/Linux: Ctrl + Shift + R
Mac: Cmd + Shift + R


📍 ÉTAPE 3: Tester (30 secondes)
───────────────────────────────────────────────────────

1. Aller sur /blog

2. Cliquer sur n'importe quel article

3. L'article devrait maintenant s'afficher avec:
   ✅ Titre
   ✅ Image de couverture
   ✅ Contenu complet
   ✅ Tags
   ✅ FAQ en bas de page
   ✅ Bouton "Retour au blog"

═══════════════════════════════════════════════════════
VÉRIFICATION CONSOLE (F12)
═══════════════════════════════════════════════════════

Avant le fix:
❌ function public.get_blog_post_by_slug does not exist
OU
❌ Article non trouvé

Après le fix:
✅ 🔍 Fetching blog post "xxx" via SQL function...
✅ (pas d'erreur)

═══════════════════════════════════════════════════════
SI ÇA NE FONCTIONNE TOUJOURS PAS
═══════════════════════════════════════════════════════

1. Ouvrir la console (F12) sur /blog/assurance-taxi-2024

2. Regarder les messages:

   A. Si "Article non trouvé":
      → Vérifier: SELECT * FROM blog_posts WHERE published = true;
      → Si vide, exécuter: INSERT-24-ARTICLES-BLOG-FIXED.sql

   B. Si "function does not exist":
      → Re-exécuter FIX-BLOG-FUNCTIONS-MAINTENANT.sql
      → Vérifier: SELECT routine_name FROM information_schema.routines
                  WHERE routine_name LIKE 'get_blog%';

   C. Si "slug not found":
      → Copier le slug exact de l'URL
      → Vérifier: SELECT slug FROM blog_posts;
      → Comparer les slugs

═══════════════════════════════════════════════════════
FICHIERS CRÉÉS
═══════════════════════════════════════════════════════

📄 FIX-BLOG-FUNCTIONS-MAINTENANT.sql
   → Fichier principal à exécuter (contient tout)

📖 DIAGNOSTIC-PROBLEME-BLOG.md
   → Guide diagnostic complet

📝 START-ICI-FIX-BLOG.txt
   → Ce fichier (instructions courtes)

═══════════════════════════════════════════════════════
RÉSULTAT ATTENDU
═══════════════════════════════════════════════════════

Après le fix:

✅ /blog → Liste complète des articles
✅ /blog/assurance-taxi-2024 → Article complet avec contenu
✅ /blog/sinistre-taxi-que-faire → Article complet avec contenu
✅ Tous les 24 articles accessibles et lisibles

═══════════════════════════════════════════════════════

👉 COMMENCER PAR: Exécuter FIX-BLOG-FUNCTIONS-MAINTENANT.sql

Date: 23 octobre 2025
Temps estimé: 2 minutes
