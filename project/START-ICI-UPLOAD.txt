╔═══════════════════════════════════════════════════════════╗
║  📤 UPLOADER MAINTENANT (2 MINUTES)                      ║
╚═══════════════════════════════════════════════════════════╝

🔴 LE PROBLÈME:
Le code corrigé est sur votre ordinateur mais pas encore 
sur le serveur IONOS de production.

═══════════════════════════════════════════════════════════

✅ LA SOLUTION (2 ÉTAPES):

┌───────────────────────────────────────────────────────┐
│ 1. SQL (30 SEC)                                       │
└───────────────────────────────────────────────────────┘

Ouvrir: https://supabase.com/dashboard/project/drohhxrkoequjphvabvq/sql

Copier-coller ce SQL:

DELETE FROM backlink_campaigns 
WHERE id NOT IN (
  SELECT DISTINCT ON (name) id
  FROM backlink_campaigns
  ORDER BY name, created_at ASC
);

SELECT 'OK: ' || COUNT(*) || ' campagne' FROM backlink_campaigns;

Cliquer RUN ▶
Résultat doit être: "OK: 1 campagne"

┌───────────────────────────────────────────────────────┐
│ 2. UPLOAD FTP (2 MIN)                                 │
└───────────────────────────────────────────────────────┘

Avec FileZilla:

1. Ouvrir FileZilla
2. Connecter à IONOS (ftp.taxiassur.com)
3. Naviguer vers /httpdocs
4. Uploader TOUT le contenu de /dist
5. Attendre fin upload

═══════════════════════════════════════════════════════

✅ VÉRIFIER (30 SEC):

1. Ouvrir: taxiassur.com/backoffice/backlink-automation
2. Ctrl+Shift+R (vider cache)
3. Vérifier: 1 seule campagne visible
4. Cliquer radio, puis "Lancer Automation"
5. ✅ Confirmation puis succès

═══════════════════════════════════════════════════════

📁 FICHIERS:

• START-ICI-UPLOAD.txt (CE FICHIER)
• DEPLOYER-MAINTENANT-3-ETAPES.txt (guide détaillé)
• EXECUTE-MAINTENANT.sql (SQL à copier)
• /dist/ (dossier à uploader)

═══════════════════════════════════════════════════════

⚠️  IMPORTANT:

Le build est DÉJÀ FAIT ✅
Il faut juste:
1. Exécuter le SQL (30 sec)
2. Uploader /dist (2 min)

╚═══════════════════════════════════════════════════════╝
