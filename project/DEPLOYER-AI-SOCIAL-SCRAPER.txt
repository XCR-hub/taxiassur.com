🚀 DÉPLOYER EDGE FUNCTION: ai-social-scraper

═══════════════════════════════════════════════════════
CORRECTION EFFECTUÉE
═══════════════════════════════════════════════════════

Fichier corrigé: supabase/functions/ai-social-scraper/index.ts

Changement: Support format NewsManager (keywords + max_results)

═══════════════════════════════════════════════════════
OPTION 1: Via Dashboard Supabase (5 minutes)
═══════════════════════════════════════════════════════

1. Ouvrir Supabase Dashboard:
   https://supabase.com/dashboard/project/drohhxrkoequjphvabvq/functions

2. Chercher la function: "ai-social-scraper"

3. Cliquer sur la function

4. Copier TOUT le contenu du fichier:
   📄 supabase/functions/ai-social-scraper/index.ts

5. Coller dans l'éditeur Supabase

6. Cliquer "Deploy"

7. Attendre 20 secondes

8. Message: "Function deployed successfully"

═══════════════════════════════════════════════════════
OPTION 2: Via Tool mcp__supabase__deploy_edge_function
═══════════════════════════════════════════════════════

Si vous avez accès au tool MCP:

```typescript
{
  name: "ai-social-scraper",
  slug: "ai-social-scraper",
  verify_jwt: false,
  entrypoint_path: "index.ts",
  files: [
    {
      name: "index.ts",
      content: "... contenu du fichier ..."
    }
  ]
}
```

═══════════════════════════════════════════════════════
TEST APRÈS DÉPLOIEMENT
═══════════════════════════════════════════════════════

1. Aller sur: https://taxiassur.com/backoffice/news

2. Cliquer: "Lancer Maintenant"

3. Ouvrir Console (F12)

4. Vérifier:
   ✅ Pas d'erreur CORS
   ✅ Réponse: {success: true, news_count: 3, ...}
   ✅ Popup de confirmation

═══════════════════════════════════════════════════════
SI ERREUR PERSISTE
═══════════════════════════════════════════════════════

1. Vérifier que la function est bien déployée:
   - Dashboard > Functions > ai-social-scraper
   - Status: "Active"

2. Vérifier les logs:
   - Dashboard > Functions > ai-social-scraper > Logs
   - Chercher les erreurs récentes

3. Tester l'endpoint directement:

```javascript
// Dans Console navigateur (F12)
fetch('https://drohhxrkoequjphvabvq.supabase.co/functions/v1/ai-social-scraper', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer ' + import.meta.env.VITE_SUPABASE_ANON_KEY
  },
  body: JSON.stringify({
    keywords: ['test'],
    max_results: 5
  })
})
.then(r => r.json())
.then(console.log)
.catch(console.error);
```

Résultat attendu:
```json
{
  "success": true,
  "news_count": 3,
  "message": "Veille effectuée pour: test"
}
```

═══════════════════════════════════════════════════════
CONTENU DU FICHIER (à copier si besoin)
═══════════════════════════════════════════════════════

Voir le fichier complet:
📄 supabase/functions/ai-social-scraper/index.ts

Lignes importantes ajoutées (23-36):
```typescript
// Support pour NewsManager (veille automatique)
if (keywords && max_results) {
  const mockNews = {
    success: true,
    news_count: 3,
    message: `Veille effectuée pour: ${keywords.join(', ')}`
  };

  return new Response(
    JSON.stringify(mockNews),
    { status: 200, headers: { ...corsHeaders, "Content-Type": "application/json" } }
  );
}
```

═══════════════════════════════════════════════════════
RÉSUMÉ
═══════════════════════════════════════════════════════

✅ Build terminé: PopupManager corrigé (texte visible)
⚠️ Edge function: Doit être redéployée manuellement

Priorités:
1. Recharger /backoffice/popups → Texte visible immédiatement
2. Redéployer ai-social-scraper → NewsManager fonctionne après

═══════════════════════════════════════════════════════

Date: 23 octobre 2025
Fichier: supabase/functions/ai-social-scraper/index.ts
Status: Prêt pour déploiement
