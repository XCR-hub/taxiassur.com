╔══════════════════════════════════════════════════════════════════╗
║  🔧 TESTER GÉNÉRATION IA - GUIDE RAPIDE                          ║
║  Diagnostic précis de l'erreur "Aucun contenu généré"          ║
╚══════════════════════════════════════════════════════════════════╝

🎯 OBJECTIF:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Identifier PRÉCISÉMENT pourquoi la génération IA ne fonctionne pas
en testant chaque composant individuellement.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ MÉTHODE 1: PAGE DIAGNOSTIC HTML (RECOMMANDÉ)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Ouvrir le fichier avec navigateur:
   → Double-clic sur: TEST-GENERATION-IA-DIRECT.html
   → OU drag & drop dans navigateur

2. La page s'ouvre avec 4 indicateurs:
   ✅/❌ Supabase
   X Templates Viraux
   ✅/❌ OpenAI API
   ✅/❌ Edge Function

3. Lire les logs automatiques en bas:
   → Vérifications effectuées au chargement
   → Messages clairs sur ce qui manque

4. Clic sur "Lancer Test Génération"
   → Test complet en temps réel
   → Logs détaillés affichés
   → Réponse API complète visible

5. Analyser résultat:

   CAS A: ✅ GÉNÉRATION RÉUSSIE
   → Message: "✅ GÉNÉRATION RÉUSSIE !"
   → Template utilisé affiché
   → Contenu généré visible
   → Action: Retour backoffice, ça fonctionne !

   CAS B: ❌ Templates Viraux: 0
   → Message: "❌ AUCUN template viral en base !"
   → Action: Exécuter PEUPLER-VILLES-ET-TEMPLATES-MAINTENANT.sql
   → Supabase Dashboard → SQL Editor → Run

   CAS C: ❌ OPENAI_API_KEY non configurée
   → Message: "→ Clé OpenAI non configurée dans Supabase Secrets"
   → Action: Dashboard → Settings → Secrets → Ajouter OPENAI_API_KEY
   → Obtenir clé: platform.openai.com/api-keys

   CAS D: ❌ Edge Function non accessible
   → Message: "❌ Edge Function non accessible (404)"
   → Action: Vérifier Edge Functions déployées
   → Dashboard → Edge Functions → Chercher ai-viral-content-generator

   CAS E: ❌ Clé OpenAI invalide (401)
   → Message: "→ Clé OpenAI invalide"
   → Action: Regénérer clé sur platform.openai.com
   → Remplacer dans Supabase Secrets

   CAS F: ❌ Rate limit (429)
   → Message: "→ Rate limit OpenAI dépassé"
   → Action: Attendre quelques minutes OU ajouter crédit

AVANTAGES:
✅ Diagnostic visuel en temps réel
✅ Logs clairs et colorés
✅ Réponse API complète affichée
✅ Identifie précisément le problème
✅ Pas besoin backoffice
✅ Fonctionne hors ligne (sauf tests API)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ MÉTHODE 2: DIAGNOSTIC SQL (TECHNIQUE)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Ouvrir Supabase Dashboard
   → https://supabase.com/dashboard

2. Menu: SQL Editor → New query

3. Copier/coller contenu de:
   → DIAGNOSTIC-GENERATION-IA-SOCIAL.sql

4. Clic "Run" / "Exécuter"

5. Lire résultats:

   TEST 1: Table viral_templates existe ?
   → ✅ Table viral_templates existe
   → ❌ Table viral_templates MANQUANTE

   TEST 2: Combien de templates ?
   → ✅ Templates viraux: 10 trouvés
   → ❌ AUCUN template viral en base !

   TEST 3: Liste templates
   → Affiche tous templates avec scores

   TEST 4: Fonction RPC existe ?
   → ✅ Fonction get_viral_template existe
   → ❌ Fonction get_viral_template MANQUANTE

   TEST 5: Test fonction RPC
   → Retourne combien de templates

   RÉSUMÉ FINAL:
   → Liste problèmes identifiés
   → Actions requises

AVANTAGES:
✅ Diagnostic complet base de données
✅ Vérifie structure tables
✅ Teste fonctions RPC
✅ Affiche données réelles

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ MÉTHODE 3: CONSOLE NAVIGATEUR (DÉVELOPPEUR)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Ouvrir backoffice social media:
   → https://taxiassur.com/backoffice/social-media

2. Ouvrir console (F12) → Tab "Network"

3. Clic "Générer avec IA"

4. Dans Network, chercher requête:
   → ai-viral-content-generator

5. Clic dessus → Tab "Response"

6. Lire erreur exacte:

   Exemple réponse:
   {
     "success": false,
     "error": "No viral template found"
   }

   → Problème: Templates manquants
   → Solution: Exécuter SQL peupler templates

   Exemple réponse:
   {
     "success": false,
     "error": "OPENAI_API_KEY not configured"
   }

   → Problème: Clé OpenAI manquante
   → Solution: Configurer dans Secrets

AVANTAGES:
✅ Erreur exacte retournée par API
✅ Diagnostic précis
✅ Voit requête complète envoyée

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 CHECKLIST RÉSOLUTION:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

□ 1. Vérifier templates viraux en base:
   → SQL Editor: SELECT COUNT(*) FROM viral_templates;
   → Résultat attendu: 10
   → Si 0: Exécuter PEUPLER-VILLES-ET-TEMPLATES-MAINTENANT.sql

□ 2. Vérifier fonction RPC:
   → SQL Editor: SELECT get_viral_template('conseil');
   → Doit retourner 1+ template
   → Si erreur: Migration 20251020100000 manquante

□ 3. Vérifier clé OpenAI configurée:
   → Dashboard → Settings → Edge Functions → Secrets
   → Chercher: OPENAI_API_KEY
   → Valeur: sk-proj-XXXXXXXXXX
   → Si manquante: Ajouter depuis platform.openai.com

□ 4. Vérifier Edge Function déployée:
   → Dashboard → Edge Functions
   → Chercher: ai-viral-content-generator
   → Status: Devrait être "Active"
   → Si absente: Problème déploiement

□ 5. Vérifier crédit OpenAI:
   → platform.openai.com/account/billing
   → Balance: Min $0.50 disponible
   → Si insuffisant: Ajouter crédit

□ 6. Tester avec page diagnostic:
   → Ouvrir TEST-GENERATION-IA-DIRECT.html
   → Lancer test
   → Analyser résultat

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 RÉSOLUTION PROBLÈMES COURANTS:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PROBLÈME: "No viral template found"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Cause: Table viral_templates vide

Solution:
1. Dashboard → SQL Editor
2. Copier PEUPLER-VILLES-ET-TEMPLATES-MAINTENANT.sql
3. Run
4. Vérifier: SELECT COUNT(*) FROM viral_templates;
5. Résultat: 10
6. Retester génération

PROBLÈME: "OPENAI_API_KEY not configured"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Cause: Clé OpenAI pas dans Secrets Supabase

Solution:
1. Aller sur platform.openai.com/api-keys
2. Login
3. "Create new secret key"
4. Nom: "TaxiAssur Prod"
5. Copier clé: sk-proj-XXXXXXXXXX
6. Dashboard Supabase → Settings → Secrets
7. Add new secret
8. Name: OPENAI_API_KEY
9. Value: sk-proj-XXX (votre clé)
10. Save
11. Attendre 30s (restart Edge Functions)
12. Retester génération

PROBLÈME: "401 Unauthorized" (OpenAI)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Cause: Clé OpenAI invalide

Solution:
1. platform.openai.com/api-keys
2. Révoquer ancienne clé
3. Créer nouvelle clé
4. Remplacer dans Supabase Secrets
5. Retester

PROBLÈME: "429 Rate Limit"
━━━━━━━━━━━━━━━━━━━━━━━━━━

Cause: Quota OpenAI dépassé

Solution:
1. Attendre 1-2 minutes
2. OU ajouter crédit sur platform.openai.com/billing
3. OU augmenter limite dépense mensuelle
4. Retester

PROBLÈME: "404 Not Found" (Edge Function)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Cause: Edge Function pas déployée

Solution:
1. Dashboard → Edge Functions
2. Vérifier ai-viral-content-generator existe
3. Si absente: Problème déploiement
4. Contacter support Supabase

PROBLÈME: Timeout (>30s)
━━━━━━━━━━━━━━━━━━━━━━━

Cause: OpenAI API lente

Solution:
1. Réessayer
2. Vérifier connexion internet
3. Vérifier status OpenAI: status.openai.com

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 EXEMPLE RÉSULTAT RÉUSSI:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Page diagnostic affiche:

Supabase: ✅
Templates Viraux: 10
OpenAI API: ✅
Edge Function: ✅

Logs:
✅ Supabase connecté
✅ 10 templates viraux trouvés
✅ Edge Function accessible
✅ GÉNÉRATION RÉUSSIE !
   Template utilisé: Conseil du Jour - Expert
   Potentiel viral: 2.5M vues
   Score humanisation: 94%
   Posts générés: 2

Contenu généré:
💡 Saviez-vous que 8 taxis sur 10 payent TROP CHER...
(suite visible)

→ SUCCÈS ! Retour sur backoffice social media
→ Génération IA fonctionne maintenant ✅

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 RÉSUMÉ 10 SECONDES:

PROBLÈME:
Génération IA ne fonctionne pas

CAUSE PROBABLE:
Templates viraux manquants en base

SOLUTION:
1. Ouvrir TEST-GENERATION-IA-DIRECT.html
2. Clic "Lancer Test"
3. Lire erreur exacte
4. Suivre instruction affichée
5. (Probablement) Exécuter SQL peupler templates
6. Retester
7. ✅ Fonctionne !

TEMPS: 5-10 minutes

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Date: 20 octobre 2025 - 23h15
Fichiers: TEST-GENERATION-IA-DIRECT.html (diagnostic visuel)
          DIAGNOSTIC-GENERATION-IA-SOCIAL.sql (diagnostic SQL)

🔧 OUVREZ TEST-GENERATION-IA-DIRECT.HTML
   POUR VOIR EXACTEMENT LE PROBLÈME ! 🔧
