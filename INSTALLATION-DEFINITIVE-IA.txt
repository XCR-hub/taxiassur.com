╔════════════════════════════════════════════════════════════╗
║  🚀 INSTALLATION DÉFINITIVE IA 4000 MOTS                  ║
╚════════════════════════════════════════════════════════════╝

⚠️  IMPORTANT : Exécute les migrations dans l'ORDRE EXACT


┌────────────────────────────────────────────────────────────┐
│ MÉTHODE 1 : Si tu n'as JAMAIS exécuté les migrations      │
└────────────────────────────────────────────────────────────┘

Dans Supabase SQL Editor :

1️⃣ Copie/colle le contenu de :
   supabase/migrations/20251024013000_connect_blog_cron_to_ai_generator.sql
   
   ➡️ Clique "Run"

2️⃣ Copie/colle le contenu de :
   supabase/migrations/20251024017000_fix_policies_safe.sql
   
   ➡️ Clique "Run"


┌────────────────────────────────────────────────────────────┐
│ MÉTHODE 2 : Si tu as DÉJÀ exécuté une migration           │
└────────────────────────────────────────────────────────────┘

Dans Supabase SQL Editor :

1️⃣ Exécute UNIQUEMENT :
   supabase/migrations/20251024017000_fix_policies_safe.sql
   
   Cette migration :
   - Drop les anciennes policies
   - Crée les nouvelles avec des noms uniques
   - Ne plante JAMAIS (ultra-safe)


┌────────────────────────────────────────────────────────────┐
│ ÉTAPE 2 : Déployer l'Edge Function                        │
└────────────────────────────────────────────────────────────┘

Terminal local :
$ cd /tmp/cc-agent/58094969/project
$ supabase functions deploy process-content-queue

✅ Résultat attendu : "Deployed Function process-content-queue"


┌────────────────────────────────────────────────────────────┐
│ ÉTAPE 3 : Vérification                                     │
└────────────────────────────────────────────────────────────┘

Dans Supabase SQL Editor :

-- Vérifier que la table existe
SELECT COUNT(*) FROM content_generation_queue;

-- Vérifier les policies
SELECT policyname, roles, cmd, qual
FROM pg_policies
WHERE tablename = 'content_generation_queue';

✅ Tu devrais voir 2 policies :
   - service_role_full_access_queue
   - anon_read_completed_queue


┌────────────────────────────────────────────────────────────┐
│ ÉTAPE 4 : Test                                             │
└────────────────────────────────────────────────────────────┘

-- Générer un article
SELECT generate_daily_blog_post();

✅ Résultat attendu :
   "✅ Article créé: ASSURANCE TAXI à Lyon (Queue: 1, Log: 15)"

-- Vérifier la queue
SELECT id, type, keyword, city, status
FROM content_generation_queue
ORDER BY created_at DESC;

✅ Tu devrais voir 1 ligne avec status = 'pending'


┌────────────────────────────────────────────────────────────┐
│ ÉTAPE 5 : Traiter la queue (Manuellement pour tester)     │
└────────────────────────────────────────────────────────────┘

Option A - Via Supabase Dashboard :
1. Va dans "Edge Functions"
2. Clique sur "process-content-queue"
3. Clique "Invoke" avec body : {}

Option B - Via curl :
curl -X POST \
  https://TON_PROJET.supabase.co/functions/v1/process-content-queue \
  -H "Authorization: Bearer TON_SERVICE_KEY" \
  -H "Content-Type: application/json"


┌────────────────────────────────────────────────────────────┐
│ ÉTAPE 6 : Vérifier le résultat final                      │
└────────────────────────────────────────────────────────────┘

-- Vérifier que la queue est traitée
SELECT id, type, status, LENGTH(result::TEXT) as result_length
FROM content_generation_queue
WHERE id = 1;

✅ status devrait être 'completed'
✅ result_length devrait être > 10000

-- Vérifier l'article enrichi
SELECT 
  title,
  slug,
  LENGTH(content) as nb_caracteres,
  LENGTH(content)/5 as nb_mots_approx,
  featured_image IS NOT NULL as has_image,
  array_length(tags, 1) as nb_tags
FROM blog_posts
ORDER BY created_at DESC
LIMIT 1;

✅ Résultat attendu :
   title: "Pourquoi ASSURANCE TAXI est..."
   nb_caracteres: 25000+
   nb_mots_approx: 5000
   has_image: true
   nb_tags: 3+


┌────────────────────────────────────────────────────────────┐
│ ÉTAPE 7 : Automatiser (Make.com gratuit)                  │
└────────────────────────────────────────────────────────────┘

1. Crée un compte sur make.com
2. Nouveau scénario
3. Ajoute module "Schedule" : toutes les 5 minutes
4. Ajoute module "HTTP - Make a Request" :
   - URL : https://TON_PROJET.supabase.co/functions/v1/process-content-queue
   - Method : POST
   - Headers :
     * Authorization: Bearer TON_SERVICE_KEY
     * Content-Type: application/json
   - Body : {}
5. Active le scénario

✅ Make.com traitera la queue automatiquement toutes les 5 min !


╔════════════════════════════════════════════════════════════╗
║  ✅ SYSTÈME COMPLET ACTIF !                               ║
╚════════════════════════════════════════════════════════════╝

✨ Chaque jour à 10h : Article basique créé + ajouté à la queue
✨ Toutes les 5 min : Queue traitée = articles enrichis avec 4000 mots IA
✨ Résultat : Contenu professionnel avec FAQ, images, métadonnées SEO

📖 Guide détaillé : SOLUTION-SIMPLE-SANS-PGNET.md

