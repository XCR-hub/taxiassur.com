╔══════════════════════════════════════════════════════════════╗
║  🚨 FIX URGENT: EXÉCUTER CE SQL MAINTENANT (30 SEC)         ║
╚══════════════════════════════════════════════════════════════╝

ERREUR ACTUELLE:
❌ "Could not find the 'contacted_at' column"

SOLUTION (30 SECONDES):

1. Ouvrir: https://supabase.com/dashboard/project/drohhxrkoequjphvabvq/sql

2. Copier-coller CE CODE:

-- Ajouter colonnes manquantes
ALTER TABLE backlink_opportunities
ADD COLUMN IF NOT EXISTS contacted_at timestamptz,
ADD COLUMN IF NOT EXISTS last_contact_date timestamptz,
ADD COLUMN IF NOT EXISTS email_sent_count integer DEFAULT 0;

-- Index
CREATE INDEX IF NOT EXISTS idx_backlink_opportunities_contacted_at 
ON backlink_opportunities(contacted_at);

-- Vérification
SELECT 
  'COLONNES AJOUTÉES ✅' as resultat,
  COUNT(*) as total,
  COUNT(contacted_at) as avec_date
FROM backlink_opportunities;

3. Cliquer "Run" ▶

4. Vérifier résultat: "COLONNES AJOUTÉES ✅"

5. Retourner dashboard → Ctrl+Shift+R → Tester

✅ ERREUR 400 DISPARAÎT!

══════════════════════════════════════════════════════════════

APRÈS CE FIX (optionnel mais recommandé):

Exécuter les 4 autres SQL pour activer système complet:

1. CREATE-EMAIL-TEMPLATES-TABLE.sql (templates emails)
2. WORKFLOW-AUTOMATION-PRODUCTION.sql (crons auto)
3. NETTOYER-OPPORTUNITES-CONCURRENTES.sql (10 bons sites)
4. Code DELETE doublon campagne

Guide: EXECUTER-MAINTENANT-5-SQL.txt

══════════════════════════════════════════════════════════════

⏱️ TEMPS: 30 SECONDES

╚══════════════════════════════════════════════════════════════╝
