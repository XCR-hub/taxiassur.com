name: Deploy Cloudflare Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cloudflare-pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      VITE_CAPTCHA_PROVIDER: turnstile
      VITE_TURNSTILE_SITE_KEY: "${{ vars.VITE_TURNSTILE_SITE_KEY || secrets.VITE_TURNSTILE_SITE_KEY || '0x4AAAAAAEAauofq7vsai3yI' }}"
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Security secret scan
        run: npm run security:scan-secrets

      - name: Client compliance guard
        run: npm run verify:client-compliance

      - name: Backoffice auth guard
        env:
          SKIP_LIVE_BACKOFFICE_AUTH_CHECK: '1'
        run: npm run verify:backoffice-auth

      - name: Deployment independence guard
        run: npm run verify:deployment-independence

      - name: SEO leadership guard
        env:
          SKIP_LIVE_SEO_CHECK: '1'
        run: npm run verify:seo-leadership

      - name: SEO quality audit
        run: npm run seo:quality-audit

      - name: Build and validate Cloudflare output
        run: npm run build:cloudflare

      - name: Check Cloudflare secrets
        id: cloudflare
        shell: bash
        run: |
          if [ -z "$CLOUDFLARE_ACCOUNT_ID" ] || [ -z "$CLOUDFLARE_API_TOKEN" ]; then
            echo "::error::Cloudflare deployment is not configured: missing CLOUDFLARE_ACCOUNT_ID or CLOUDFLARE_API_TOKEN."
            exit 1
          fi
          echo "ready=true" >> "$GITHUB_OUTPUT"

      - name: Align Cloudflare AI robots access
        if: steps.cloudflare.outputs.ready == 'true'
        env:
          CLOUDFLARE_ZONE_NAME: taxiassur.com
          CLOUDFLARE_PAGES_PROJECT: taxiassur
          CLOUDFLARE_ZONE_ID: ${{ secrets.CLOUDFLARE_ZONE_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_BOT_MANAGEMENT_API_TOKEN || secrets.CLOUDFLARE_API_TOKEN }}
        run: npm run cloudflare:ai-robots -- --soft

      - name: Deploy to Cloudflare Pages production
        if: steps.cloudflare.outputs.ready == 'true'
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name taxiassur --branch main

      - name: Verify production site
        if: steps.cloudflare.outputs.ready == 'true'
        env:
          SITE_URL: https://taxiassur.com
          EXPECTED_COMMIT: ${{ github.sha }}
          PUBLIC_MIRROR_COUNT_TOLERANCE: '5'
          PUBLIC_MIRROR_GSC_COUNT_TOLERANCE: '25'
        run: |
          for attempt in $(seq 1 24); do
            if npm run verify:production; then
              exit 0
            fi
            echo "Production verification failed on attempt ${attempt}; retrying in 15s..."
            sleep 15
          done
          npm run verify:production

      - name: Verify self-hosted public source priority
        if: steps.cloudflare.outputs.ready == 'true'
        env:
          SITE_URL: https://taxiassur.com
        run: npm run verify:self-hosted-first

      - name: Verify production backoffice auth
        if: steps.cloudflare.outputs.ready == 'true'
        env:
          SITE_URL: https://taxiassur.com
        run: npm run verify:backoffice-auth

      - name: Verify production SEO signals
        if: steps.cloudflare.outputs.ready == 'true'
        env:
          SITE_URL: https://taxiassur.com
        run: npm run verify:seo-leadership

      - name: Verify public publication automations
        if: steps.cloudflare.outputs.ready == 'true'
        env:
          SITE_URL: https://taxiassur.com
          PUBLIC_MIRROR_COUNT_TOLERANCE: '5'
          PUBLIC_MIRROR_GSC_COUNT_TOLERANCE: '25'
        run: npm run verify:publications
